{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "write_file",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[PreToolUse] Writing file — validating no secrets in content'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "write_file",
        "hooks": [
          {
            "type": "command",
            "command": "npx tsc --noEmit 2>&1 | tail -5 || true"
          }
        ]
      }
    ],
    "PreCommit": [
      {
        "type": "command",
        "command": "grep -r 'sk-ant-\\|sk-\\|SUPABASE_SERVICE\\|STRIPE_SECRET\\|ELEVENLABS' --include='*.ts' --include='*.tsx' --include='*.js' src/ && echo 'SECRET DETECTED — COMMIT BLOCKED' && exit 1 || echo 'PreCommit: No secrets found — safe to commit'"
      }
    ],
    "SessionStart": [
      {
        "type": "command",
        "command": "echo '=== SaintSal Labs Platform Session Started ===' && echo 'Repo: saintsallabs-production' && echo 'Stack: Next.js 14 | Supabase | Stripe | Claude API | Vercel' && git log --oneline -5 2>/dev/null || true"
      }
    ],
    "Notification": [
      {
        "type": "command",
        "command": "echo '[SaintSal Platform] Notification: $NOTIFICATION_MESSAGE'"
      }
    ]
  },
  "permissions": {
    "allow": [
      "Read(**)",
      "Write(src/**)",
      "Write(public/**)",
      "Bash(git *)",
      "Bash(npm *)",
      "Bash(npx *)",
      "Bash(curl *)",
      "Bash(supabase *)"
    ],
    "deny": [
      "Write(.env.local)",
      "Write(.env)",
      "Bash(rm -rf *)"
    ]
  }
}
